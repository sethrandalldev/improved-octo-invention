export interface Workspace {
  id: string;
  title: string;
  description: string;
  created_by: string;
}

export interface User {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
}
